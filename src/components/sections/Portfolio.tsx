import PropertyFilters from "../ui/PropertyFilters";

const Portfolio = () => {
  return (
    <div className="relative mb-35">
      <PropertyFilters />
    </div>
  );
};

export default Portfolio;
