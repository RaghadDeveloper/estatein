import { valuesData } from "../../data/aboutData";

const Values = () => {
  return <div>
    <div>
      {
        valuesData.map((element,index)=>{
          return 
        })
      }
    </div>
  </div>;
};

export default Values;
