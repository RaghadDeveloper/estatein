export interface imageProps{
  source:string;
  source-alternative:string;
  alternative:string;
}
export interface SliderProps {
  cards: React.ReactNode[];
  cardsPerView?: number;
  spaceBetween?: number;
}
